{
  "icon": "TextIndent",
  "lang": "javascript",
  "script": "function process(data) {\n  const text = data && data.selection;\n  if (typeof text !== 'string') {\n    throw new TypeError('selection must be a string');\n  }\n\n  return text\n    .split(/(\\r\\n|\\n|\\r)/)\n    .map((part, index) => (index % 2 === 0 && part.trim() !== '' ? `    ${part}` : part))\n    .join('');\n}\n",
  "locales": {
    "en": {
      "name": "Indent Lines",
      "description": "Indent each non-blank line by four spaces.",
      "tags": ["whitespace", "multiline"]
    },
    "zh-CN": {
      "name": "增加行缩进",
      "description": "为每个非空白行增加四个空格缩进",
      "tags": ["空白", "多行"]
    }
  },
  "sort": 20640
}
