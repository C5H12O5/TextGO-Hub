{
  "icon": "Sparkle",
  "lang": "javascript",
  "script": "function process(data) {\n  const text = data && data.selection;\n  if (typeof text !== 'string') {\n    throw new TypeError('selection must be a string');\n  }\n\n  return text.normalize('NFC');\n}\n",
  "locales": {
    "en": {
      "name": "Normalize Unicode",
      "description": "Normalize selected text to Unicode NFC form.",
      "tags": ["normalization"]
    },
    "zh-CN": {
      "name": "Unicode 规范化",
      "description": "将选中文本规范化为 Unicode NFC 形式",
      "tags": ["规范化"]
    }
  },
  "sort": 10500
}
