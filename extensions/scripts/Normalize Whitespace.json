{
  "icon": "TextColumns",
  "lang": "javascript",
  "script": "function process(data) {\n  const text = data && data.selection;\n  if (typeof text !== 'string') {\n    throw new TypeError('selection must be a string');\n  }\n\n  return text\n    .split(/(\\r\\n|\\n|\\r)/)\n    .map((part, index) =>\n      index % 2 === 0 ? part.replace(/^[ \\t]+|[ \\t]+$/g, '').replace(/[ \\t]+/g, ' ') : part\n    )\n    .join('');\n}\n",
  "locales": {
    "en": {
      "name": "Normalize Whitespace",
      "description": "Trim spaces and tabs at each line edge and collapse internal runs to a single space.",
      "tags": ["whitespace", "normalization", "multiline"]
    },
    "zh-CN": {
      "name": "规范化空白",
      "description": "去除每行首尾的空格和制表符，并将行内连续空格和制表符合并为一个空格",
      "tags": ["空白", "规范化", "多行"]
    }
  },
  "sort": 20620
}
