{
  "icon": "SortAscending",
  "lang": "javascript",
  "script": "function process(data) {\n    const lines = data.selection.match(/[^\\r\\n]+/g) || [];\n    return lines\n        .sort((a, b) =>\n            a.localeCompare(b, undefined, {\n                numeric: true,\n                sensitivity: 'base',\n            }),\n        )\n        .join('\\n');\n}\n",
  "locales": {
    "en": {
      "name": "Sort Lines Ascending",
      "description": "Sort lines ascending (natural order, case-insensitive)",
      "tags": ["convert", "multiline"]
    },
    "zh-CN": {
      "name": "升序排序行",
      "description": "按升序排列行（自然顺序，忽略大小写）",
      "tags": ["转换", "多行"]
    }
  },
  "sort": 1767624102958
}
