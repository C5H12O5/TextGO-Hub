{
  "icon": "SortDescending",
  "lang": "javascript",
  "script": "function process(data) {\n    const lines = data.selection.match(/[^\\r\\n]+/g) || [];\n    return lines\n        .sort((a, b) =>\n            b.localeCompare(a, undefined, {\n                numeric: true,\n                sensitivity: 'base',\n            }),\n        )\n        .join('\\n');\n}\n",
  "locales": {
    "en": {
      "name": "Sort Lines Descending",
      "description": "Sort lines descending (natural order, case-insensitive)",
      "tags": ["convert", "multiline"]
    },
    "zh-CN": {
      "name": "降序排序行",
      "description": "按降序排列行（自然顺序，忽略大小写）",
      "tags": ["转换", "多行"]
    }
  },
  "sort": 1767624103058
}
